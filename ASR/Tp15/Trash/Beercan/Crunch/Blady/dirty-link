#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>


        int main()
        {
                unsigned char x = 1;
                unsigned char y;
                y=x+1;
                while (x < y) {
                  x++;
                  y=x+1;
		}
                printf("%d\n", x);
                exit(0);
        }

