#include <stdio.h>
#include <stdlib.h>

int main(int argc, char ** argv) {
	int x;
	int min=0;
	int max=3;
	printf("Entrer un entier:\n"); 
 	scanf("%d", &x);
	while ()
		if (( x < min ) && ( x > max )) {
		
		}
		else {
			min=min+3;
			max=max+3;
		}
/*	9 < x < 12
	(9+12)/2 = 10,5
	Si x<10,5, il est plus proche de 9
	Sinon il est plus proche de 12
*/
	return EXIT_SUCCESS;
}
